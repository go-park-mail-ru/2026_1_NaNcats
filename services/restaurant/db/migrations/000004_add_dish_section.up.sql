-- Раздел меню ресторана для блюда (напр. "Супы", "Десерты").
-- Заполняется сидами/владельцем; NULL — раздел не задан (фронт сгруппирует эвристикой).
ALTER TABLE "dish"
	ADD COLUMN section TEXT
		CHECK (char_length(section) <= 60);
