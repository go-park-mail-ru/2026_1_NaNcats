-- Поднимаем общий потолок использований до 1000 для всех промокодов, чтобы
-- глобальный max_uses не блокировал акции; per-user-ограничение «один раз»
-- продолжает работать через promocode_usage.
UPDATE "promocode" SET max_uses = 1000;
